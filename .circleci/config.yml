version: 2.1

# Basic config to initialize CircleCI
workflows:
  hello-workflow:
    jobs:
      - hello-job

jobs:
  hello-job:
    docker:
      - image: cimg/base:stable
    steps:
      - checkout
      - run:
          name: Say Hello
          command: |
            echo "Hello from CircleCI!"
            echo "Repository: ${CIRCLE_PROJECT_REPONAME}"
            echo "Build: ${CIRCLE_BUILD_NUM}"
            echo "✅ CircleCI is working!"
